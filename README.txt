Hi,

To see the project working, please run it on a local server (localhost) as the P5.js library does not work without it.

For example, if you have NodeJS or Python installed, then from this directory you could run the following commands:

python -m http.server
python3 -m http.server
http-server